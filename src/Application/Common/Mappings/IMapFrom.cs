using AutoMapper;

namespace Clean_Architecture_Asp_Net_Core_Starter.Application.Common.Mappings
{
    public interface IMapFrom<T>
    {   
        void Mapping(Profile profile) => profile.CreateMap(typeof(T), GetType());
    }
}
